#pragma once
#include <vector>
/*
 * Given an array A, find the length of the smallest sub-array
 * that contains both the minimum and maximum elements of the array.
 */

int MinMaxSubArrayBruteForce(const std::vector<int>& vec);
int MinMaxSubArray(const std::vector<int>& vec);



